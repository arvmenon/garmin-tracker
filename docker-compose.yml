services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8009:8000"
    environment:
      - DATABASE_URL=postgresql+psycopg2://${APP_DB_USER:-garmin_app}:${APP_DB_PASSWORD:-garmin_app}@db:5432/${APP_DB_NAME:-garmin_tracker}
      - ALLOWED_ORIGINS=http://localhost:4010,http://127.0.0.1:4010,http://frontend:4010
      - DEBUG_LOGGING=${DEBUG_LOGGING:-false}
    depends_on:
      - db

  frontend:
    image: node:20
    working_dir: /app
    command: sh -c "npm install && npm run build && npm run start"
    volumes:
      - ./frontend:/app
      - frontend_build:/app/.next
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:8009
      - NEXT_PUBLIC_DEBUG_LOGGING=${DEBUG_LOGGING:-false}
    ports:
      - "4010:4010"
    depends_on:
      - api

  db:
    image: postgis/postgis:15-3.4
    entrypoint: ["/bin/sh", "/docker/postgres-entrypoint.sh"]
    command: ["postgres"]
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      DEBUG_LOGGING: ${DEBUG_LOGGING:-false}
      APP_DB_NAME: ${APP_DB_NAME:-garmin_tracker}
      APP_DB_USER: ${APP_DB_USER:-garmin_app}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD:-garmin_app}
      READONLY_DB_USER: ${READONLY_DB_USER:-garmin_readonly}
      READONLY_DB_PASSWORD: ${READONLY_DB_PASSWORD:-garmin_readonly}
      NETDATA_DB_USER: ${NETDATA_DB_USER:-netdata}
      NETDATA_DB_PASSWORD: ${NETDATA_DB_PASSWORD:-netdata}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./docker/db-init:/docker-entrypoint-initdb.d
      - ./docker/postgres-entrypoint.sh:/docker/postgres-entrypoint.sh:ro
    ports:
      - "5432:5432"

volumes:
  pgdata:
  frontend_build:
